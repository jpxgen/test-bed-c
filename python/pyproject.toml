[project]
name = "pager"
version = "0.1.0"
description = "Splits sequences into pages; ships with a command-line tool"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pytest==9.0.2"]

[build-system]
requires = ["uv_build>=0.8.17,<0.9.0"]
build-backend = "uv_build"

[tool.pytest.ini_options]
testpaths = ["tests"]
